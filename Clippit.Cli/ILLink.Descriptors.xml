<LinkerDescriptor>
    <!--
    ILLink / NativeAOT root descriptors for Clippit.Cli.

    All DocumentFormat.OpenXml types (3.5.1, net10.0, IsTrimmable=true) and
    SixLabors.ImageSharp word/HTML paths are handled by static analysis —
    no manual roots needed for those.

    SlideMasterPart and SlideLayoutPart are kept because OpenXml's internal
    part-loading mechanism may resolve these types via a type-keyed dictionary
    rather than a direct static reference, which static analysis cannot trace.
    All other Packaging/Presentation types are statically referenced in CLI code
    and are kept by the trimmer automatically.
  -->

    <assembly fullname="DocumentFormat.OpenXml">
        <type fullname="DocumentFormat.OpenXml.Packaging.SlideMasterPart" preserve="all" />
        <type fullname="DocumentFormat.OpenXml.Packaging.SlideLayoutPart" preserve="all" />
    </assembly>
</LinkerDescriptor>
